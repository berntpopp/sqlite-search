{
  "title": "Help & Documentation",
  "subtitle": "Learn how to use SQLite Search effectively",
  "sections": [
    {
      "id": "overview",
      "header": "What is SQLite Search?",
      "icon": "mdi-information-outline",
      "color": "primary",
      "content": {
        "intro": "SQLite Search is a desktop application for performing full-text searches on SQLite databases using FTS5.",
        "features": [
          "Fast full-text search powered by SQLite FTS5",
          "Browse mode for viewing entire tables with server-side pagination",
          "Support for boolean operators (AND, OR, NOT)",
          "Wildcard and phrase search capabilities",
          "Dark/light theme with persistent preferences",
          "Search history with restore functionality"
        ],
        "tech": "Built with Vue 3, Vuetify 3, and Electron"
      }
    },
    {
      "id": "getting-started",
      "header": "Getting Started",
      "icon": "mdi-rocket-launch-outline",
      "color": "success",
      "content": {
        "intro": "Follow these steps to start searching your database:",
        "steps": [
          "Click the database button in the header to select your SQLite file",
          "Choose an FTS5 table from the dropdown (only FTS5 tables are shown)",
          "Select which columns to include (TEXT columns auto-selected by default)",
          "Choose Search mode to find specific terms, or Browse mode to view all data",
          "View results in the table below - click rows for details"
        ]
      }
    },
    {
      "id": "database",
      "header": "Database Requirements",
      "icon": "mdi-database-outline",
      "color": "warning",
      "content": {
        "intro": "SQLite Search requires databases with FTS5 (Full-Text Search 5) virtual tables.",
        "requirements": [
          "Database must contain at least one FTS5 virtual table",
          "Regular SQLite tables will not appear in the selector",
          "FTS5 tables are created using: CREATE VIRTUAL TABLE ... USING fts5(...)"
        ],
        "note": "If no tables appear after selecting your database, verify it contains FTS5 virtual tables using any SQLite browser tool."
      }
    },
    {
      "id": "search-syntax",
      "header": "Search Syntax (FTS5)",
      "icon": "mdi-code-tags",
      "color": "info",
      "content": {
        "intro": "FTS5 supports powerful search operators:",
        "examples": [
          { "syntax": "protein", "description": "Simple term search" },
          { "syntax": "\"cell membrane\"", "description": "Exact phrase (use quotes)" },
          { "syntax": "protein AND enzyme", "description": "Both terms must match" },
          { "syntax": "gene OR protein", "description": "Either term matches" },
          { "syntax": "cancer NOT benign", "description": "Exclude term" },
          { "syntax": "bio*", "description": "Prefix wildcard (matches biology, biochemistry)" },
          { "syntax": "protein NEAR/5 enzyme", "description": "Terms within 5 words" }
        ],
        "tip": "Combine operators for complex queries: (gene OR protein) AND NOT obsolete"
      }
    },
    {
      "id": "browse-mode",
      "header": "Browse Mode",
      "icon": "mdi-table-eye",
      "color": "teal",
      "content": {
        "intro": "Browse mode allows you to view entire tables without searching:",
        "features": [
          "Toggle between Search and Browse using the mode buttons above the search field",
          "Browse mode uses server-side pagination for efficient handling of large tables",
          "Navigate through pages using the pagination controls at top and bottom",
          "Sort by any column - sorting is performed server-side for performance",
          "View row details and copy data just like in search mode"
        ],
        "performance": "Browse mode efficiently handles tables with hundreds of thousands of rows by fetching only the current page from the database.",
        "tip": "Use Browse mode when you want to explore your data without a specific search term in mind."
      }
    },
    {
      "id": "results",
      "header": "Working with Results",
      "icon": "mdi-table-search",
      "color": "secondary",
      "content": {
        "intro": "Results are displayed in an interactive table (both Search and Browse modes):",
        "features": [
          "Click column headers to sort results (multi-sort supported in Search mode)",
          "Use the filter icon on column headers to filter results (Search mode)",
          "Use the eye icon to view full record details in a dialog",
          "Use the copy icon to copy field values or entire rows to clipboard",
          "Adjust visible columns with the COLUMNS button",
          "Clear results with the CLEAR button (Search mode)"
        ],
        "pagination": "Both modes include pagination controls at the top and bottom of the table for easy navigation."
      }
    },
    {
      "id": "history",
      "header": "Search History",
      "icon": "mdi-history",
      "color": "primary",
      "content": {
        "intro": "Your searches are automatically saved for quick access:",
        "features": [
          "Click the history icon in the header to view past searches",
          "Each entry shows the search term, result count, and context",
          "Click RESTORE to re-run a previous search",
          "Delete individual entries or clear all history"
        ],
        "note": "History is stored locally and persists across sessions."
      }
    },
    {
      "id": "settings",
      "header": "Theme & Settings",
      "icon": "mdi-cog-outline",
      "color": "default",
      "content": {
        "intro": "Customize the application to your preferences:",
        "options": [
          { "icon": "mdi-weather-sunny", "name": "Theme Toggle", "description": "Switch between light and dark mode" },
          { "icon": "mdi-format-text", "name": "Auto-select TEXT", "description": "Automatically select TEXT columns for searching" },
          { "icon": "mdi-refresh", "name": "Reset State", "description": "Clear all selections and return to initial state" }
        ],
        "persistence": "All preferences are saved automatically and restored on next launch."
      }
    },
    {
      "id": "troubleshooting",
      "header": "Troubleshooting",
      "icon": "mdi-lifebuoy",
      "color": "error",
      "content": {
        "intro": "Common issues and solutions:",
        "issues": [
          { "problem": "No tables appear", "solution": "Ensure your database contains FTS5 virtual tables, not regular tables" },
          { "problem": "No search results", "solution": "Try simpler terms, use wildcards (*), or check column selection" },
          { "problem": "Syntax error", "solution": "Check FTS5 syntax - special characters may need quoting" },
          { "problem": "App is slow", "solution": "Large result sets take time - try narrowing your search" }
        ]
      }
    }
  ]
}
