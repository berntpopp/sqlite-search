# Disable script blocking in pnpm
ignore-scripts=false

# Use hoisted node_modules layout for electron-builder compatibility
# pnpm's default symlinked layout causes dist-electron/ to be excluded from asar
node-linker=hoisted
